# Timeweb App Platform (опционально)

Если деплой идёт через **GitHub Actions на VPS**, используйте [DEPLOY-GITHUB-ACTIONS.md](./DEPLOY-GITHUB-ACTIONS.md).

Ниже — альтернатива: встроенный автодеплой Timeweb **без** GitHub Actions (корневой `docker-compose.yml`, без `volumes`). Подробности: [документация Timeweb](https://timeweb.cloud/docs/apps/deploying-with-docker-compose).
